# cargo-deny configuration — run with `cargo deny check advisories`.
#
# Every ignore below is a tracked exception, not a dismissal. When an entry's
# upstream fix lands, remove it so CI starts enforcing again.

[advisories]
ignore = [
    # rsa Marvin timing side-channel — no fixed release exists. Pulled in
    # transitively by ssh-commander-core 0.2.2 → russh 0.61 → ssh-key → rsa
    # for RSA key support (the russh 0.43 → 0.61 upgrade fixed the previous
    # RUSTSEC-2026-0153/0154 russh advisories but surfaces this one). Low
    # practical risk for an interactive SSH client.
    # TRACKED: drop when the rsa crate ships a constant-time fix.
    { id = "RUSTSEC-2023-0071", reason = "no fix available; transitive via russh 0.61 / ssh-key; review-by 2026-11-27" },

    # Unmaintained / unsound (not exploitable here). All transitive via
    # ssh-commander-core; revisit when its dependency tree is next overhauled.
    { id = "RUSTSEC-2025-0052", reason = "async-std discontinued — transitive via ssh-commander-core/suppaftp; review-by 2026-11-27" },
    { id = "RUSTSEC-2024-0436", reason = "paste unmaintained — transitive proc-macro, build-time only; review-by 2026-11-27" },
]

[licenses]
# The dependency graph is intentionally broad because the app ships SSH,
# SFTP, Postgres, Parquet, and UniFFI support. Keep the allowlist explicit so
# `cargo deny check` catches a new license rather than silently accepting it.
allow = [
    "0BSD",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-1-Clause",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "CC0-1.0",
    "ISC",
    "LGPL-2.1-or-later",
    "MIT",
    "MIT-0",
    "MPL-2.0",
    "Unicode-3.0",
    "Unlicense",
    "Zlib",
]

# AGPL is this app's own license — allow it only for the workspace crate, not
# blanket-allowed, so a transitively-pulled AGPL dependency (a real problem for
# App Store distribution) is still denied.
exceptions = [
    { name = "agent-ssh", allow = ["AGPL-3.0-or-later"] },
]

# Only accept crates from the crates.io registry. Anything from an unknown
# registry or a git source is denied rather than warned, so a dependency can't
# be silently swapped for an off-registry or git build.
[sources]
unknown-registry = "deny"
unknown-git = "deny"
